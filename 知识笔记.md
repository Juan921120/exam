# 知识笔记

## 不同数据量级的存储方案选择

### 1. 千级以内（当前你的场景：900题）

| 方案 | 适用场景 |
|------|----------|
| **JSON 文件** | 零成本，直接 import 使用，加载到内存毫无压力 |
| **LocalStorage** | 浏览器端，简单 key-value，容量约 5MB |
| **Taro Storage** | 小程序端，setStorageSync/getStorageSync，足够 |

**结论**：当前 JSON 方案完全够用，无需引入任何数据库。

### 2. 千级 ~ 万级（1,000 ~ 10,000）

| 方案 | 适用场景 |
|------|----------|
| **SQLite** | 本地离线优先，需要按条件筛选（分类、难度等） |
| **微信云数据库** | 需要多端同步、用户答题记录云端存储 |
| **MongoDB / MySQL** | 需要后台管理题目、运营分析 |

**分水岭**：当需要**按字段索引查询**时，JSON 遍历就慢了，SQLite 的 B+ 树索引优势明显。

### 3. 万级 ~ 十万级（10,000 ~ 100,000）

| 方案 | 适用场景 |
|------|----------|
| **MySQL / PostgreSQL** | 关系型数据，需要多表关联 |
| **MongoDB** | 文档型，题目数据结构经常变化 |
| **Redis** | 热数据缓存 + 随机抽题 |

### 4. 十万级 ~ 百万级（100,000 ~ 1,000,000）

| 方案 | 适用场景 |
|------|----------|
| **MySQL + 分表** | 按月份/分类分表 |
| **MongoDB + 分片** | 水平扩展 |
| **ElasticSearch** | 全文搜索 |
| **ClickHouse** | 分析型数据仓库 |

### 5. 百万级以上（> 1,000,000）

| 方案 | 适用场景 |
|------|----------|
| **分布式数据库** | TiDB / CockroachDB |
| **数据湖** | Hive + Spark |
| **实时计算** | Flink / Kafka Streams |

## 快速决策树

```
数据量 < 1万？
  ├─ Yes: JSON够用，题目不常更新？
  │        ├─ Yes: 继续用JSON
  │        └─ No: 需要筛选查询？→ SQLite
  └─ No: 需要多端同步？→ 微信云数据库 / MySQL云服务
        └─ No: MySQL / PostgreSQL单机够用
```

**总结**：900题用 JSON + Taro Storage 是最优解，无需改动。万级以上再考虑引入 SQLite 或云数据库。
